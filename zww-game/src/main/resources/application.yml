server:
  port: 80 #项目端口

gameserver:
   port: 9081
   host: localhost
   threads: 1000

web: 
 upload-path: data
 

spring:
  datasource:
      url: jdbc:mysql://127.0.0.1:3306/rest?autoReconnect=true&useUnicode=true&characterEncoding=utf8&zeroDateTimeBehavior=convertToNull&useSSL=false
      username: root
      password: admins
      filters: log4j,wall,mergeStat


logging:
  level.root: info
  level.com.stylefeng: debug
  path: logs/
  file: guns-rest.log